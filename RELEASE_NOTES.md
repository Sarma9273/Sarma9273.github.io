# Portfolio V2 — Clean Repository Release

This package is a clean replacement for the previous 2023 GitHub Pages repository.

## Corrected in this release

- Clean repository root with `src/`, `public/`, and `.github/` preserved.
- Workflow filename standardised to `.github/workflows/deploy.yml`.
- All 389 internal npm-registry URLs replaced with public `registry.npmjs.org` URLs.
- Project-level `.npmrc` added.
- Pre-install repository verification added through `npm run verify`.
- Old V1 archive removed from the new repository package.
- GitHub Pages workflow configured for the `main` branch and Astro `dist` output.

## Website address

`https://sarma9273.github.io/`
